[English](./README.md) | **中文**

# SimpleZip

![macOS](https://img.shields.io/badge/macOS-13.0%2B-000000?logo=apple&logoColor=white)
![Swift](https://img.shields.io/badge/Swift-SwiftUI-F05138?logo=swift&logoColor=white)
![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)

**原生 macOS 压缩管理器。** 把压缩包当文件夹浏览并直接编辑，
几下点击打包、转换或拆分 ZIP · 7z · RAR · TAR · DMG · gz · bz2 · xz，
粘入下载链接即可边下边解。另支持 tar.zst、ISO、CAB、CPIO、XAR、PKG
等只读格式，以及 GPG 签名与验证。

无订阅、无数据收集。内置官方 7-Zip 引擎，开箱即用。

➡️ **[下载最新版本](https://github.com/chiba233/SimpleZip/releases)** ·
[项目主页](https://github.com/chiba233/SimpleZip)

---

## 📸 界面一览

| | |
|---|---|
| ![浏览文件](https://raw.githubusercontent.com/chiba233/SimpleZip/main/demo/mainView.png) | ![浏览压缩包](https://raw.githubusercontent.com/chiba233/SimpleZip/main/demo/archiveView.png) |
| **像 Finder 一样浏览** —— 现代原生窗口。 | **把压缩包当文件夹打开** —— 直接走目录树，不用先解压。 |
| ![创建压缩包](https://raw.githubusercontent.com/chiba233/SimpleZip/main/demo/addArchiveView.png) | ![解压](https://raw.githubusercontent.com/chiba233/SimpleZip/main/demo/unzipView.png) |
| **创建** —— 格式、密码、模板、预检，一步完成。 | **解压** —— 操作前展示预检概要。 |
| ![活动中心](https://raw.githubusercontent.com/chiba233/SimpleZip/main/demo/ActivityView.png) | ![校验和](https://raw.githubusercontent.com/chiba233/SimpleZip/main/demo/hashView.png) |
| **活动中心** —— 每个操作都有实时进度。 | **校验和** —— 一键算哈希、复制结果。 |

![设置](https://raw.githubusercontent.com/chiba233/SimpleZip/main/demo/SettingView.png)

> *设置 —— 全部收在一个整洁的原生窗口里。*

---

## ✨ 功能

- 📂 **把压缩包当文件夹浏览。** 双击 `.zip` / `.7z` 直接走目录树，不用先
  「全部解压」。本地文件浏览里文件夹还能像 Finder 列表视图一样原位展开。
- 👁 **不解压直接打开里面的文件。** 双击压缩包里的文档，用它默认的 App 打开
  临时副本；用完不留任何垃圾。
- ✍️ **在压缩包里直接编辑。** 从 ZIP/7z 里打开一个文件、改完保存，改动会写回
  压缩包——不用解压再重打包。也能增 / 删 / 重命名条目，或把文件直接拖进
  打开的压缩包。
- 🗜 **打包、转换、拆分。** 选文件 → 选格式 → 完成（带密码 ZIP、紧凑 7z、
  `.tar.gz`）。还能把压缩包转换成别的格式，或把大文件拆分成分卷、之后再
  合并——字节级一致。
- 🖐 **直接拖到 Finder。** 把文件从压缩包拖到桌面，放手那一刻才真正解压。
- ↩️ **撤销 / 重做。** 移动、重命名、复制、副本、删除、拆分、合并、转换、
  连同改权限——⌘Z 都能撤回（安全撤销，绝不静默覆盖你的数据）。
- 🔍 **搜索、过滤、对比。** 搜索框认高级 token（`*.swift size:>1MB
  encrypted:true modified:<7d regex:…`），过滤条件可保存，还能对比两个
  压缩包——或拿文件夹跟压缩包比——把结果导成 JSON / CSV / Markdown。
- 🔄 **列表自动刷新。** 在 Finder 里增删文件，SimpleZip 的视图自己更新——
  不用手动刷新，而且保留你的选区。
- 🙈 **隐藏文件独立分组。** 显示隐藏文件时，它们折叠到独立分组中，不在主列表中混杂。
- 🔤 **分组、调大小、重命名、改权限。** 按种类或时间分组、选舒服的行高、
  列表内直接重命名（选中按 Return），右键查看 / 修改 Unix 权限与属主。
- 🔐 **签名与验签（可选）。** 把压缩包封进一个带签名的 `.siz` 单文件让签名
  随包走，或用 `.szs` 清单就地给一组文件签名。还能加密给指定的人和 / 或
  一个口令，交给对方一个会自我说明的「安全投递包」。
- 🍎 **Finder 集成。** 在 Finder 里右键任何文件就能算哈希、打包，连主窗口都
  不用开。
- 🌍 **说你的语言。** English、简体中文、繁體中文、日本語、한국어、Русский、
  Deutsch、Français、Español、ไทย。

### 🧠 搜索、AI 与自动化（macOS 26 + Spotlight）

- **Spotlight 搜索压缩包内容。** 打开过的归档条目会本地记录，之后在 Spotlight 中搜索
  文件名可直接定位到对应压缩包内的文件，点开即跳转。历史发布和完成的任务也可搜索。
- **设置页自然语言搜索。** 在设置搜索框中输入「别再问我是否删除」「关闭 GPG」等，
  直接跳转到对应设置项。安全敏感设置不会被自动修改。
- **本地 AI 解释报告。** Apple Intelligence 可用时，安全报告、签名验证、空间分析等
  结果旁提供解释按钮，在本机生成可编辑文本框中的说明，供审阅后使用。不接触加密内容。
- **创建/解压时的实时提示。** 对话框内展示针对当前文件的提示——单文件打包建议、
  同名冲突、磁盘空间不足——以及压缩后大小预估。
- **文件浏览器中的 AI 建议。** 展开文件或压缩包行时展示一句话定性和操作建议——
  算校验值、用合适的 App 打开、取特定文件。仅限白名单文件夹，Mac 空闲时预读，
  不上传任何数据。
- **会自适应的设置与工具栏。** 创建/解压对话框可一键「套用推荐设置」,工具栏也会按
  当前文件重排操作,把你最可能要用的那一步放在最前——AI 关时按习惯,AI 开时更聪明。
  密码、加密强度和目标路径绝不会被自动改动。

### 🧰 进阶工具，要用的时候都在

- **格式转换** —— 一步把 `.rar` 重新打成 `.7z`、把 `.zip` 打成加密 `.siz`
  （右键 → 转换…）。批量转换支持统一输出目录、已存在相同跳过（按指纹比对，
  重跑不堆副本）、失败自动重试，以及可选的转换后测试。
- **拆分与合并** —— 把大文件切成 `partN` 分卷再拼回去，字节级一致，对齐
  7-Zip 的 Split / Combine。
- **归档注释** —— 直接在浏览器里读取*并编辑* ZIP 的整包注释。
- **模板与预设** —— 从现成配方开始（GitHub Release ZIP、极限压缩 7z、加密
  投递、不带 `node_modules` 的源码 tarball…），或保存自己的命名默认值。
- **预检** —— 创建前 dry-run（文件数、大小、哪些被排除）、解压前概要（含磁盘
  空间警示），以及发布前一键**发布包检查**（完整性测试、可疑路径、垃圾、
  空目录、SHA-256，外加重新打包也不变的**结构指纹**）。检查 `.app` 会核对
  Info.plist、代码签名与 Gatekeeper；DMG 核对拖装布局；XIP 报告签名信任。
- **解压预设** —— 解压对话框里的一键配方（干净解压 / 保守安全 / 解完整理），
  由一组新开关组成：解到与压缩包同名的文件夹、去掉单层包装目录、冲突自动
  改名（目标已有文件零接触）、完成后在 Finder 显示，以及完整成功后可选把
  原包移到废纸篓。
- **从网址边下边解** —— 把指向压缩包的 `http(s)` 链接粘进地址栏，SimpleZip 会
  *边下载边*把它解到本地，整包不必先存下来。仅支持可流式的格式与服务器——它会
  先探测,而不是假装支持。
- **流式快速解压** —— 解压 ZIP 与 tar 系压缩包时,可选的流式模式顺序读取:对大包
  和网络卷明显更快。
- **发布助手** —— 打包（可复现、排垃圾）→ 检查 → SHA256SUMS → 可选签名，
  一条**可断点续跑**的流；**工作区预设**把整套配置存成名字。它还维护一份
  **发布账本**（每次跑都记下版本、哈希、结构指纹）、能**把本次发布与上次对比**、
  可顺手丢一份机器可读的 `release-manifest.json`，并有一道可配置的**质量门**——
  发版前对可疑路径、垃圾、缺校验文件、未签名产物给出警告或直接拦下。
- **可复现压缩** —— 创建对话框一个开关，让同一份输入每次打包都得到字节级
  一致的 ZIP/7z（同一个 SHA-256）。为 GitHub Release 和可验证构建而生；
  和**导出 SHA256SUMS** 正好配套。
- **直观的安全评级（A / B / C）。** 路径安全报告顶部展示一目了然的风险等级，
  按最严重的一项发现定级，不因大量轻微发现而误报高风险。纯规则判定，AI 不参与。
- **损坏归档数据救援** —— 越过 CRC 错误继续解压、ZIP 中央目录没了就扫本地头；
  能读出来的全落进 `名字 (rescued)` 文件夹，**原包绝不被动**，且安全检查照跑
  （损坏归档正是头号恶意输入）。
- **批量体检** —— 选一批归档（或一个文件夹扫顶层），一趟过完：打开、完整性
  测试、扫可疑路径 / macOS 垃圾 / 加密条目 / 缺卷 / 只读格式 / 批内重复。
- **敏感与配置文件识别。** 对归档条目进行文件名扫描，归类私钥/凭据、许可证、
  配置文件和脚本。仅匹配文件名，不检查内容。
- **近似重复识别** —— 在字节相同的查重之外，把版本 / 改名 / 副本
  （`report.docx`、`report (1).docx`、`report_v2.docx`、`报告 副本.docx`）归到
  一组，并标「字节相同的副本」还是「相似版本」。
- **可复现性深度报告。** 对文件夹执行双重打包，验证生成的两个归档是否字节级一致
  （同一 SHA-256），并列出各因素在不同格式下的保留情况。
- **发布目录检查 & 快速验证** —— 确认一个发布文件夹是否完整可验证：SHA256SUMS
  覆盖与真实哈希、逐文件核对 `.szs` 清单、**独立洁净室验证**(随包公钥真能验随包
  签名、绝不碰你自己的钥匙串)、DMG 是否真有内容、孤儿文件。「快速验证」只读
  文件名做一次秒级完整性检查。
- **随时心里有数** —— 你正看着的归档被别的 app 移动 / 改写时弹横幅；长任务在
  SimpleZip 处于后台时收尾，可选弹系统通知。
- **空间分析与查重** —— 一眼看压缩包里最大的文件、目录与扩展名占比；扫描
  文件夹里的**疑似重复归档**（按指纹归组，跨格式也认）；在压缩包**内容里
  搜索**——只搜文本文件，临时区即用即删。
- **校验文件** —— 为选中文件生成 GNU 兼容的 `SHA256SUMS`，也能验证别人发来的
  `SHA256SUMS` / `.sha256` / `.md5` / `.sfv`，逐文件通过 / 失败记录在活动中心。
- **真·命令行** —— 在 设置 → 自动化 安装 `simplezip`，终端里跑
  `check` / `compare` / `create` / `verify` / `open` / `doctor`，真实退出码——
  外加 `--json` 机器可读输出、`--quiet` / `--verbose`、打错有「你是不是想…」
  提示、shell 补全和一份 man page。`create` 学会了
  `--level` / `--exclude-junk` / `--reproducible` / `--encrypt`（加密口令走
  `SIMPLEZIP_PASSWORD` 环境变量或不回显终端输入，绝不进命令行参数）。同一套
  内置引擎；完成的命令同样记录进活动中心。
- **快捷指令与 Siri** —— 从「快捷指令」app 或 Siri 驱动 SimpleZip：解压、创建、
  校验 checksum、对比归档、在包内搜索、不解压直接检查、无界面跑发布助手。每次
  运行都在活动中心标为快捷指令来源；`.szs` 签名只在交互下做、绝不无人值守运行。
- **URL 动作** —— 其他 App 和脚本可以通过 `simplezip://check?path=…` /
  `compare` / `open` 请求 SimpleZip 执行动作；一定先弹写明动作与完整路径的
  确认框。
- **写入有验证、失败有退路** —— 每次改写归档都先在工作副本上测试、再原子
  替换原包；在最后一道闸被拦下的改写（外部改动、验证失败）会把工作副本存进
  **恢复区**而不是烧掉。大操作开工前先核对磁盘空间。
- **会话密码** —— 同一个包本会话只问一次口令：测试、批量任务、Finder 自动
  解压都会静默复用你验证过的口令（只存内存——退出即忘；持久化的只有钥匙串里
  的预设密码）。
- **长任务有保护** —— 任务运行时挡住空闲睡眠；带任务退出会先问（也可以选
  「完成后退出」）；重任务按可配置的并发上限排队（带可见的「等待中」分组）；
  还有一次性的**完成后睡眠**开关，最后一个任务收尾时让 Mac 入睡。
- **批量工具** —— 一次测试 / 转换 / 重命名多个条目，找重复文件，清理 macOS
  元数据垃圾（`.DS_Store` / `__MACOSX`）。
- **分卷感知** —— `.001 / .002 / partN.rar` 识别成一组，合并前点名缺哪几卷
  ——还有**缺卷搜索**：另选一个目录扫描，找到的卷复制回来，找齐自动合并并
  测试产物。
- **权限与属主** —— 本地文件的 Unix 权限列和右键 chmod / chown。
- **性能测试。** 测试 7-Zip 引擎在当前 Mac 上的压缩与解压速度。
- **活动中心** —— 每个操作可重跑、可复制等价命令、可导出诊断包。可一键暂停 /
  恢复整条队列，按来源筛选（应用内 / CLI / 快捷指令 / URL / Finder），任意报告
  可重新打开（跨重启保留），macOS 26 上还能**用一句话**圈出你想看的任务视图。
- **标签页与多窗口** —— 用原生窗口标签页打开多个压缩包和文件夹。
- **快速预览、显示简介、打开方式** —— Finder 的基本功，列表里就有。

## 🚀 一分钟上手

1. 从 [Releases](https://github.com/chiba233/SimpleZip/releases) **下载**最新
   DMG，把 **SimpleZip** 拖进「应用程序」。
2. **首次打开：** 双击 App 即可——已通过 Developer ID 签名和公证，Gatekeeper
   直接放行。
3. 一个简短的**欢迎助手**带你过几个偏好设置、确认压缩引擎就绪。全都可以跳过，
   以后再改。

拖入压缩包，或使用 **文件 → 打开** 即可开始使用。

## 🗂 能打开什么、能做什么

| 格式 | 打开 | 解压 | 创建 | 说明 |
|---|:---:|:---:|:---:|---|
| ZIP | ✓ | ✓ | ✓ | 可选 AES-256 密码 |
| 7z | ✓ | ✓ | ✓ | 高压缩率 |
| RAR | ✓ | ✓ | ✓\* | 打开始终可用；*创建*需要 RARLAB 的 `rar`（可选附加组件） |
| TAR | ✓ | ✓ | ✓ | |
| gz / bz2 / xz | ✓ | ✓ | ✓ | 单文件压缩 |
| tgz / tar.gz | ✓ | ✓ | ✓ | 打开时直接进入内层 tar |
| zst / tar.zst | ✓ | ✓ | — | Zstandard，只读——内置 7-Zip 能解码但没有 zstd 编码器 |
| ISO / CAB / CPIO / XAR / PKG | ✓ | ✓ | — | 只读——镜像、CAB、安装器包都能浏览和解压 |
| DMG | ✓ | ✓ | ✓ | Apple 磁盘镜像（浏览时只读挂载） |
| XIP | ✓ | ✓ | — | Apple 签名归档（Xcode 等）——解压走 Apple 自家 `xip` 工具，会校验签名 |
| `.gpg` / `.pgp` / `.asc` / `.key` | ✓ | ✓ | ✓ | GPG 加密文件解密后原位打开；密钥材料会提示导入钥匙串。「创建」= 右键 → 加密为 `.gpg` |
| `.siz` | ✓ | ✓ | ✓ | 签名容器——压缩包和它的签名合成一个文件 |
| `.szs` | ✓（验签） | 原位 | ✓ | 签名清单——*验证*散落在文件夹里的文件并以虚拟目录浏览；它没打包过任何东西，自然没有「解包」 |
| 分卷组（`.001`、`.z01`、`.r00`、`partN.rar`） | ✓ | ✓ | — | 打开第一卷即可 |

> 不能原位改写的格式（tar 系、RAR、上面的只读家族）打开后状态栏会挂一个
> **「只读」徽章**并讲明原因——右键「转换格式」一步转成 ZIP / 7z 就能编辑。

## 🔏 签名与验证

可选功能——使用 GPG 签名确认文件来源与完整性：

- **`.siz` —— 签名容器。** 压缩包与签名合为一个文件。收件人打开即可看到签名者、
  验证结果。支持用收件人公钥和/或共享口令加密内容。
- **`.szs` —— 签名清单。** 不重新打包，直接对现有文件生成签名清单，之后可逐个
  验证文件是否被改动。右键选区 → **创建签名清单…**。
- **安全投递。** `.siz` 内含防篡改的收件人说明，描述内容与验证步骤。

密钥在 **设置 → GPG** 管理：五分组钥匙串（本机私钥、智能卡 / OpenPGP token
密钥、他人公钥），逐密钥信任等级、加子密钥、可选硬件 token 支持。SimpleZip
自己创建的密钥放在独立的私有钥匙串里，与系统 `~/.gnupg` 隔离，而且永远不存
你的 passphrase——交给标准 macOS 弹窗处理。完整密码学设计与威胁模型见
**[SECURITY.zh-CN.md](./SECURITY.zh-CN.md)**。

> 使用签名需要 GPG。缺失时 SimpleZip 的 GPG 设置页会给一行
> `brew install gnupg pinentry-mac`，还有实时健康检查确认一切就绪。

## 🛡 隐私与数据安全
<a id="safety-model"></a>

- **什么都不上传。** 没有账号、没有统计，压缩包工作流不碰网络。仅有的网络
  活动全部由你主动发起：可选的更新检查（Sparkle）、设置 → GPG 密钥服务器区
  的搜索 / 发布（由 GnuPG 自带的 `dirmngr` 执行，发布公钥前必有显式确认）、
  以及你自己运行的可选 RAR 后端安装器。
- **绝不静默覆盖。** 解压、粘贴、拖放、创建撞上同名都走同一个冲突对话框——
  替换、两者保留、合并（Finder 式）或整体替换（tar 式）、跳过、或*仅内容不同
  时替换*。覆盖先写临时文件、成功才原子换入，半路失败不会两份都没了。
- **在压缩包里编辑是非破坏性的。** 把改动写回 ZIP/7z 时先做副本、再原子替换
  原包；任何一步失败，你的压缩包一个字节都不变。
- **不可信的压缩包按不可信对待。** 可疑路径、符号链接、硬链接、可执行 / 主动
  内容各自有确认门——共享机器上每一道都可以设成**始终拒绝**。
- **临时解压放在按会话加密的暂存卷里**，关掉压缩包那一刻清干净。
- **保存的密码放 macOS 钥匙串**，明文查看要过 Touch ID / 登录密码。会话记住的
  归档口令只存内存、退出即忘。SimpleZip 永远不存 GPG passphrase。
- **改写落地前先验证。** 编辑过的归档先在工作副本上做完整性测试、再原子换入；
  期间原包被外部改过的话写入会停下，你已完成的工作副本会保留在可见的恢复区。

> 完整威胁模型、`.siz` / `.szs` 密码学设计、加密暂存卷、Sparkle 更新签名方案,
> 都写在 **[SECURITY.zh-CN.md](./SECURITY.zh-CN.md)**。

## 💡 值得知道

- SimpleZip 已通过 **Developer ID 签名并公证** —— 首次启动无需右键打开，
  Gatekeeper 直接放行。
- 它**有意不开沙盒** —— 文件管理器要挂磁盘镜像、跑压缩引擎、接受全盘拖放。
- 官方 7-Zip 引擎**内置** —— ZIP/7z/TAR 等开箱即用，什么都不用装。GPG 和
  RAR 创建是可选附加组件。

## ⚙️ 设置一览

- **通用** —— 启动位置、语言、预设密码、重跑欢迎助手、偏好备份 / 恢复。
- **压缩 / 归档** —— 引擎选择、覆盖行为、Finder 自动解压、上面那些安全确认门。
- **浏览器** —— 显示隐藏文件（以及什么算隐藏）、符号链接。
- **视图** —— 行高（紧凑 / 标准 / 舒适）、显示哪些列、可选的分组默认值。
- **文件关联** —— 把 SimpleZip 设为各压缩格式的默认打开方式。
- **自动化** —— 所有「从外部驱动 SimpleZip」的东西收在一页：命令行工具、快捷指令
  动作、Spotlight 索引、`simplezip://` URL 命令、归档内容缓存、按来源的使用统计，
  以及「允许自动化使用预设密码」这个安全开关。
- **软件更新** —— 检查新版本、App 内读更新日志。
- **GPG** —— 打开签名功能、管理密钥、选默认签名密钥。
- **健康检查** —— 一眼看「一切正常吗」的面板，一键*复制诊断信息*。

## 🐞 发现 bug？有想法？

请[提一个 issue](https://github.com/chiba233/SimpleZip/issues/new/choose) ——
bug 报告和功能请求都有现成模板。App 内 **帮助 → 报告问题…** 直达。
安全相关的事请先看 [SECURITY.zh-CN.md](./SECURITY.zh-CN.md)。

---

## 📚 更多

- [English README](./README.md)
- [更新日志](./CHANGELOG.zh-CN.md) · [What's new (English)](./CHANGELOG.md)
- [中文安全策略](./SECURITY.zh-CN.md) · [Security details (English)](./SECURITY.md)
- 自动化 —— [命令行工具](./docs/CLI.zh-CN.md) · [URL scheme](./docs/URL-SCHEME.zh-CN.md) · [快捷指令与 Siri](./docs/SHORTCUTS.zh-CN.md)

## 许可证

MIT —— 见 [LICENSE](./LICENSE)。SimpleZip 是独立的单人维护软件，内置官方
7-Zip（`7zz`）二进制；许可细节见
[内置工具说明](./SimpleZip/Tools/README.md)。
