[English](./README.md) | **中文**

# SimpleZip 使用指南

**一个用起来像 Finder 的原生 macOS 压缩工具。** 把压缩包当文件夹打开、不解压就
预览里面的文件、几下点击做出 ZIP（以及 7z / RAR / TAR / DMG / gz / bz2 / xz）；
需要的时候，还能给压缩包签名、验签，让收到文件的人确认「真的是你发的、没被动过」。

没有订阅、没有数据上报、没有杂七杂八 —— 就是一个快、原生的窗口。

➡️ **[下载最新版本](https://github.com/chiba233/SimpleZip/releases)** ·
[项目主页](https://github.com/chiba233/SimpleZip)

---

## 📸 界面一览

| | |
|---|---|
| ![浏览文件](https://raw.githubusercontent.com/chiba233/SimpleZip/main/demo/mainView.png) | ![浏览压缩包](https://raw.githubusercontent.com/chiba233/SimpleZip/main/demo/archiveView.png) |
| **像 Finder 一样浏览** —— 现代原生窗口。 | **把压缩包当文件夹打开** —— 直接走目录树，不用先解压。 |
| ![活动中心](https://raw.githubusercontent.com/chiba233/SimpleZip/main/demo/ActivityView.png) | ![校验和](https://raw.githubusercontent.com/chiba233/SimpleZip/main/demo/hashView.png) |
| **活动中心** —— 每个操作都有实时进度。 | **校验和** —— 一键算哈希、复制结果。 |

![设置](https://raw.githubusercontent.com/chiba233/SimpleZip/main/demo/SettingView.png)

> *设置 —— 全部收在一个整洁的原生窗口里。*

---

## ✨ 你会喜欢它的地方

- 📂 **把压缩包当文件夹浏览** —— 双击 `.zip` / `.7z` 直接走目录树，路径不是平铺
  的长字符串，不用先「全部解压」。本地文件浏览里文件夹还能像 Finder 列表视图
  一样**原位展开**。
- 👁 **不解压直接打开里面的文件** —— 双击压缩包里的文档 / 图片 / DMG / `.app`，
  用各自默认 App 打开临时副本，用完不留垃圾。
- ✍️ **在压缩包里直接编辑** —— 从 ZIP/7z 里打开一个文件、改完保存，改动会**写回
  压缩包**，不用解压再重打包。也能往打开的压缩包里增 / 删 / 重命名条目，或直接
  把文件拖进去。
- 🗜 **打包 / 转换 / 拆分** —— 选文件 → 选格式 → 完成（带密码 ZIP、紧凑 7z、给同事
  的 `.tar.gz`）。还能把一个压缩包**转换**成别的格式，或把大文件**拆分**成分卷、
  之后再**合并**——字节级一致。
- 🖐 **直接拖到 Finder** —— 把文件从压缩包拖到桌面，放手那一刻才真正解压。
- ↩️ **撤销 / 重做** —— 移动、重命名、复制、副本、删除、拆分、合并、转换、连同
  改权限，⌘Z 都能撤回（安全撤销，绝不静默覆盖你的数据）。
- 🔍 **搜索 / 过滤 / 对比** —— 搜索框认高级 token（`*.swift size:>1MB
  encrypted:true modified:<7d regex:…`），过滤条件可保存，还能**对比两个压缩包**——
  或拿文件夹跟压缩包比——把结果导成 JSON / CSV / Markdown。
- 🔄 **列表自动刷新** —— 在 Finder 里增删文件，SimpleZip 的视图会自己更新，
  不用手动刷新，而且保留你当前的选区。
- 🙈 **隐藏文件不糊脸** —— 需要时才显示隐藏文件，而且收进一个可折叠的分组，
  不挤占列表。
- 🔤 **分组 / 调大小 / 重命名 / 改权限** —— 按种类或时间分组、选一个舒服的行高、
  直接在列表里重命名（选中按 Return），右键还能看 / 改 Unix 权限与属主。
- 🔐 **签名与验签（可选）** —— 把压缩包封进一个带签名的 `.siz` 单文件，或用
  `.szs` 清单就地给一组文件签名；还能加密给指定的人和 / 或一个密码，并以会自我
  说明的「安全投递包」交给对方。
- 🍎 **Finder 集成** —— 在 Finder 右键就能哈希文件、打包、解压到此，不用先开主窗口。
- 🌍 **多语言**（英 / 简中 / 繁中 / 日 / 韩 / 俄 / 德 / 法 / 西 / 泰）。

## 🚀 快速开始

1. 从 [Releases](https://github.com/chiba233/SimpleZip/releases) 下载最新
   DMG，把 `SimpleZip.app` 拖进 `/Applications`。
2. 首次启动：右键 App → **打开**（SimpleZip 是 ad-hoc 签名，Gatekeeper 第一次
   需要明确确认）。
3. **欢迎助手**会在首次启动时跑一次，自动检测后端（`7zz`，以及可选的 `gpg`
   和 `pinentry-mac`）。不需要 GPG 就跳过。
4. 想用 GPG 签名 / 验签功能，去**设置 → GPG**打开主开关。缺组件的话面板会
   显示完整的 `brew install` 命令。

## 🧩 安装与后端

### 7-Zip（内置）

SimpleZip 内置官方 7-Zip 26.01 universal `7zz`，位置：

```text
SimpleZip/Tools/7zz
```

同时含 `x86_64` 和 `arm64`，开发构建时会被复制进 App 资源目录。设置里可以
选择 7-Zip 后端：

- 自动（优先 App 内置 → Homebrew → `PATH`）；
- 软件内置；
- 系统安装。

你也可以装系统版本：

```bash
brew install sevenzip
```

### RAR（可选，用户本地安装）

RAR 浏览 / 解压走 7-Zip。**创建** RAR 需要 RARLAB 专有 `rar` 命令行工具。
开发或本地打包时可以跑：

```bash
./scripts/install_rar_backend.sh
```

脚本会下载 RARLAB macOS ARM 和 x64 命令行包，合成本地 universal `rar`，装到：

```text
~/Library/Application Support/SimpleZip/Tools/rar
```

这个文件被 git 忽略。**RARLAB `rar` 是专有 / shareware 软件**：要公开分发
带 RAR 后端的 App 包，先取得 RARLAB 再分发授权，不能直接打包内置。

### GPG（可选）

`.siz` 创建 / 验签和 GPG 设置面板需要它。装：

```bash
# 最少：gpg + pinentry-mac
brew install gnupg pinentry-mac
# 智能卡 / OpenPGP token 支持（可选）
brew install ykman
```

设置 → GPG 面板会跑一次活检：gpg 路径 / 版本 / `gpg-agent` 状态 /
`pinentry-mac` 解析路径，组件缺失时直接给你显示需要跑的 `brew install` 命令。

## 🗂 支持格式

| 格式                               | 浏览       | 解压 | 创建   | 说明                                                              |
|----------------------------------|----------|----|------|-----------------------------------------------------------------|
| `.zip`                           | 支持       | 支持 | 支持   | 优先 7-Zip；简单选项可回退 `/usr/bin/zip`                                 |
| `.7z`                            | 支持       | 支持 | 支持   | 需要 `7zz` / `7z`                                                 |
| `.rar`                           | 支持       | 支持 | 支持   | 浏览/解压走 7-Zip；创建需要 RARLAB `rar`                                  |
| `.tar`                           | 支持       | 支持 | 支持   | 创建走 `/usr/bin/tar`                                              |
| `.gz` / `.bz2` / `.xz`           | 支持       | 支持 | 支持   | 单文件压缩流                                                          |
| `.tgz` / `.tar.gz`               | 支持       | 支持 | 支持   | 创建走 `/usr/bin/tar`                                              |
| `.dmg`                           | 支持       | 支持 | 支持   | 通过 macOS `hdiutil` 创建和只读挂载                                      |
| `.xip`                           | 支持       | 支持 | 暂不支持 | Apple 签名归档（Xcode 等）；浏览走 7-Zip，解压走系统 `xip`（校验 Apple 签名）          |
| `.siz`                           | 支持       | 支持 | 支持   | SimpleZip 独家：GPG 签名单文件容器（tar 壳）；v3 起支持多收件人 + 对称口令加密             |
| `.szs`                           | 支持（虚拟目录） | —  | 支持   | SimpleZip 独家：GPG 签名清单（clearsign 的 JSON，记录每个文件的 SHA256） —— 不是压缩包 |
| `.001`、`.z01`、`.r00`、`partN.rar` | 支持       | 支持 | 暂不支持 | 自动归一化到首卷                                                        |

## 📂 打开文件夹和压缩包

### 打开文件夹

- 左侧栏选个人 / 下载 / 桌面 / 文档 / 应用程序 / 自定义路径等位置；
- 工具栏「打开」；
- 菜单栏 `文件 → 打开文件夹`；
- 位置栏直接输入路径。

设置 → 通用里支持「**启动位置**」自定义路径 + 失效弹窗（路径被删 / 改名时
启动会提示让你重选，不会静默回到「个人」）。

### 打开压缩包

- 在文件列表里双击；
- 菜单栏 `文件 → 打开压缩包`；
- 从 Finder 拖入；
- 从 Finder 右键 → 打开方式 → SimpleZip。

分卷压缩包可以从任意常见分卷打开，SimpleZip 会归一化到首卷：

```text
archive.002        → archive.001
bundle.z01         → bundle.zip
movie.part02.rar   → movie.part01.rar
legacy.r01         → legacy.rar
payload.7z.003     → payload.7z.001
```

`.xip`(Apple 签名归档,Xcode 等的分发格式)也能打开和解压:浏览显示容器结构,
解压走系统自带的 `xip` 工具——由系统校验 Apple 签名,拿到的是真实内容物(比如 .app)。
非 Apple 信任签名的 xip 会被系统拒绝。SimpleZip 不提供创建 xip(需要 Apple 证书)。

## 🔍 浏览压缩包

压缩包内部按真实目录树浏览。哪怕原 archive 没存目录项，SimpleZip 也会自动
合成节点，让浏览体验跟普通文件夹一致。比如压缩包里只有：

```text
Mos.app/Contents/PkgInfo
```

会显示成：

```text
Mos.app/
└── Contents/
    └── PkgInfo
```

常用操作：

- 双击目录 = 进入；
- 顶部「上一级」 = 返回；
- 表头点击 = 排序；表头拖动 = 重排列；
- 表头右键 = 跳到「设置 → 列表列」配置可见列；
- 列还能显示压缩包里存的**注释**（`.7z` / `.zip` 的 comment），有就在浏览器顶部
  展示；ZIP 的归档级注释还能**直接编辑**（横幅铅笔 / 空白处右键，原生改写 EOCD）；
- 项目右键 = 打开 / 快速预览 / 保存副本到… / 解压选中项 / 解压整个压缩包 / 测试 /
  哈希 / 编辑写回（见下）/ 对比 / 转换格式 / 发布包检查 / 在 Finder 显示源压缩包；
  多选条目还能**批量重命名**、多选压缩包能**批量测试 / 批量转换**；
- 空白处右键 = **查找重复文件**（按 大小 + CRC）、可写包还能**清理 macOS 元数据**
  （`.DS_Store` / `__MACOSX`）。

可编辑的 ZIP / 7z（真实顶层包，非嵌套 / 非临时解出）还支持**就地编辑**：
右键空白处「新建 / 粘贴」、右键条目「重命名 / 删除 / 批量重命名」，或直接把外部
文件拖进窗口。所有写回都先在临时副本上做完再原子替换原包，失败时原包字节不变；
条目名一律放在 `--` 之后传给后端，名叫 `-r` 之类的恶意条目不会被当成开关。

## 👁 不解压直接打开 / 编辑写回

双击压缩包里的普通文件时，SimpleZip 会：

1. 把该项目临时解压到系统临时目录（位于本次会话的加密临时卷）；
2. 用 macOS 默认 App 打开；
3. 关闭压缩包时清理本次产生的临时目录。

适合直接打开：

- PDF / Word / Excel / 图片 / 文本 / 代码；
- `.dmg` 磁盘映像 → 临时解出 + 只读挂载 + 以文件夹方式浏览；
- `.pkg` 安装包；
- `.app` 包目录。

**双击「打开」是只看不写**：改了临时副本不会自动回到压缩包。要把改动留在压缩包里，
用**右键 → 编辑**（针对可编辑的 ZIP / 7z）：SimpleZip 会把该条目解到临时位置、用
默认 App 打开，你保存后它**写回压缩包**（先在副本上重打包再原子替换，失败原包不变）。
加密包编辑会先要口令，避免把新条目写成未加密塞进加密包。

## 🖐 拖拽

### 从压缩包拖出

把压缩包里的文件 / 文件夹直接拖到 Finder 或其它 App —— SimpleZip 使用 macOS
的文件承诺机制：

- 拖动时不解压；
- 放手到目标位置才真正解压；
- 目标已有同名文件 = 弹同名冲突对话框（替换 / 保留两者 / 跳过 / 仅内容不同时替换），
  绝不静默覆盖。

### 在本地文件列表中拖动

- 拖到列表里某个目录行 = 移动到该目录；
- 从 Finder 拖文件进当前文件夹 = 复制；
- 拖文件 / 文件夹出去 = 让外部 App / Finder 接管。

## 📤 解压

### 整包解压

打开压缩包后点「解压」，或在文件夹里选中压缩包右键「解压到此处」。

解压前会进入选项面板：

- 默认目标 = 压缩包所在文件夹；
- 「保存到」可换位置；
- 可选密码（**预设密码**配置过的话会自动勾「使用预设密码」并把值填进
  password 字段，仍可手动改）；
- 「显示详情」展开后端实时输出；
- `.siz` 文件解压时多三行：签名状态徽章 + 签名时间 + 公钥指纹。

### 解压选中项

打开压缩包后选中一个或多个文件 / 目录，右键「解压选中项」。

路径模式：

- **保持目录结构** = 按压缩包内原路径落地；
- **仅解压文件到目标目录** = 从深层目录挑文件出来时省得手动找。

解压会先进入临时目录，再合并到目标目录 —— 同名文件冲突由 SimpleZip 处理
（询问 / 替换 / 保留两者 / 跳过 / 哈希不同时替换），不让后端静默覆盖。

进度条是尽力而为的后端输出解析，不是字节级精确。不同格式、不同后端版本可能
只提供不确定进度，或者百分比跳跃。

## 🗜 创建压缩包

在普通文件夹模式下选中文件 / 文件夹，点工具栏「添加」或菜单 `文件 →
添加到压缩包`。

创建面板支持：

- 文件名；
- 格式：ZIP / 7z / DMG / RAR / TAR / GZip / TAR.GZ / BZip2 / XZ；
- 压缩率；
- 密码（ZIP / 7z / RAR）；
- 分卷大小（ZIP / 7z / RAR）；
- 跳过 `.DS_Store`；
- 跳过 `.` 开头隐藏文件（`.env` / `.gitignore` / `.npmrc` 之类）；
- 自定义排除规则（一行一条或逗号分隔）；
- 7-Zip 高级选项：字典 / word size / 固实 / 路径模式 / 符号链接保留 /
  硬链接保留 / 共享文件压缩 / 原始参数 / 压缩后删除源文件。

「自定义排除」区域有个「**计算**」按钮：启用 `.DS_Store` / 隐藏文件 /
自定义规则后，点它会按当前选中源统计预计会被排除的普通文件数量。

不同格式的限制：

- ZIP：优先 7-Zip；后端不可用且选项简单时可回退 `/usr/bin/zip`；
- 7z：必须有 `7zz` / `7z`；
- DMG：用 `hdiutil create -format UDZO`。多选时会先把选中项放进临时
  staging，确保 DMG 顶层就是这些文件 / 文件夹；
- RAR：必须有 RARLAB `rar`；
- TAR / TAR.GZ：用 `/usr/bin/tar`；
- GZip / BZip2 / XZ：单文件格式，**不能多选**；
- ZIP 密码能力取决于后端和加密方式 —— 高强度加密优先选 7z 或 ZIP AES。

### `.siz` 单文件签名容器

勾选「**用 GPG 签名（包装成 .siz 容器）**」后，输出会变成单文件 `.siz` —— 内层
是你刚才选的压缩格式 + 签名 + 元数据一起打 tar 包。signature 不会跟 archive
脱节，邮件转发 / 网盘传输都不会丢。

设置 → GPG → 默认值 里有「签名密钥策略」：

- **静默使用默认密钥**（默认）—— 用钥匙串里标了「默认」的私钥签名，不打扰你；
- **每次询问** —— 勾签名后下方多一个 picker 让你针对当前压缩包挑用哪把私钥
  签。多密钥用户（工作 / 私人 / 项目分开签）建议开这个。Picker 出现时会自动
  滚到视野内。

签名密钥默认从你在「设置 → GPG → 钥匙串」里点过「设为默认」的那把读。

### `.siz` v3 多收件人加密（0.1.9）

`.siz` v3 在签名容器**内部**额外给内层 archive 加一层加密 —— 签名 + 元数据
的信任模型完全不变，加密只是「**叠在签名容器里面**」的可选层。创建面板上勾
「**用 GPG 签名（包装成 .siz 容器）**」之后，会多出「加密内层 archive」开关，
打开后可选三种模式：

- **只用收件人公钥** —— `gpg --encrypt --recipient <fp> ...`。任何持有匹配
  私钥的收件人都能解开。可以一次填多个 fingerprint，包括你自己的密钥（确保
  自己以后还能解）。
- **只用对称口令** —— `gpg --symmetric`。知道这串口令的人都能解开，适合不想
  / 没法用 GPG 公钥的场景。
- **组合模式** —— `gpg --symmetric --encrypt --recipient ...`。任一收件人
  私钥**或**对称口令都能解开，灵活但任一渠道泄露都会让密文可读。

收件人列表（fingerprint + UID）会作为「**声称**」记录在签好名的 metadata
里，加上一个 `hasSymmetricPassphrase: true` 的标志位（口令本身**永远不**
进 metadata）。`innerArchiveSHA256` 算的是**密文字节** —— 也就是说，没有
解密密钥的人也能凭公开的 metadata 校验容器完整性；同时攻击者哪怕事后拿到
明文，也没法重新加密出 SHA 一致的密文（gpg 每次加密的 session key 随机，
重新加密一定产生不同字节）。

解包时 SimpleZip 看到内层条目名是 `archive.<ext>.gpg` + metadata 带
`encryption` 字段，就触发解密流程：调 `gpg --decrypt`，需要 passphrase 时
通过 stdin（`--passphrase-fd 0`）喂进去，**永远不**作为命令行参数 —— `ps`
和活动监视器里看不到。解出来的明文落到跟解包同一个临时目录，解压一结束
立即清理。

详细安全推理见 [SECURITY.zh-CN.md](./SECURITY.zh-CN.md#siz-签名容器格式)。

### `.szs` 签名清单

`.szs` 跟 `.siz` 解决的是不同问题：「**给一棵保持原样放在磁盘上的文件树
签名**」 —— 发布物（App + LICENSE + README + 校验和）、镜像快照、逐文件
完整性审计这种场景。`.szs` 是一份 GPG clearsign 的 JSON 清单，跟被签名的
文件**并排**放在同一个 payload root 下，文件本身不动。

```
-----BEGIN PGP SIGNED MESSAGE-----
Hash: SHA512

{ "schema": "SimpleZip.szs", "version": 2,
  "files": [
    { "relativePath": "README.md", "size": 1234, "sha256": "abc..." },
    ...
  ],
  ... }
-----BEGIN PGP SIGNATURE-----
...
-----END PGP SIGNATURE-----
```

**创建**：在文件浏览器里多选要签的文件 / 文件夹，右键 → **创建签名清单…**。
SimpleZip 递归扫一遍选中项，按 `relativePath` 字典序排序，对每个文件流式算
SHA256，按确定性 JSON 编码序列化（`sortedKeys + prettyPrinted`），最后用
你选的私钥 `gpg --clearsign` 包成 `.szs`。默认输出路径是
`<payloadRoot>/<文件夹名>.szs`，放在签名文件旁边。

**验签**：双击任意 `.szs` 文件，SimpleZip 弹验证 sheet：

1. **签名校验** —— `gpg --status-fd 1 --decrypt` 跑 clearsign body，机器
   可读状态码（`GOODSIG` / `VALIDSIG` / `TRUST_*`）跟 `.siz` 同一套解析，
   两遍走（用户 keyring + SimpleZip 私有 ring，跟 `.siz` 一致合并）；
2. **路径安全检查** —— 即便签名通过，仍对 `relativePath` 做路径穿越拒绝
   （`..` / 绝对路径 / Windows 盘符 / UNC / 反斜杠）—— 防签名者已被攻陷的
   场景；
3. **逐文件 SHA256** —— 对每条记录解析 `<payloadRoot>/<relativePath>`，
   流式算 SHA256 跟记录值比对。结果分四类：`.match` / `.mismatch` /
   `.missing` / `.unreadable`，每条带徽标显示；不一致的可以展开看「期望
   SHA256」对「实际 SHA256」。

**虚拟目录浏览**：验证 sheet 上的「**作为虚拟目录浏览**」按钮会把 payload
root 当成普通文件夹打开，**但只显示** `.match` 的条目 + 它们的上层目录
（祖先路径）。不一致 / 缺失 / 不可读的文件，以及 payload root 里没被清单
列出的「**多余文件**」，都隐藏 —— 这样用户不会把没验过的文件误当成已验
通过的内容。地址栏显示 `/path/to/manifest.szs`，让「虚拟压缩包」的语境
明显；从虚拟目录里向上走出 payload root 范围时，过滤器自动失效。

完整格式规范见 [docs/SZS-FORMAT.md](./docs/SZS-FORMAT.md)，威胁模型见
[SECURITY.zh-CN.md](./SECURITY.zh-CN.md#szs-签名清单格式)。

## 🔐 GPG 集成

设置 → GPG 面板把 GPG 钥匙管理做齐了：

### 钥匙串五分组

按 `(来源, 是否有私钥, 是否 stub)` 切分：

- **我的密钥（本机私钥）** —— 来自 `~/.gnupg/` 的完整私钥；
- **我的密钥（智能卡 / OpenPGP token）** —— `~/.gnupg/` 里挂着卡 stub；
- **我的密钥（SimpleZip 私有 · 独立 GNUPGHOME）** —— 完整私钥在 SimpleZip
  自己的 `~/Library/Application Support/SimpleZip/gnupg/` 里（独立 0700
  目录），**不污染**用户 `~/.gnupg/`；
- **他人公钥（GPG keyring）** —— 来自 `~/.gnupg/`；
- **他人公钥（仅 SimpleZip）** —— 你专门导入到 SimpleZip 私有 ring 的他人公钥。

每行展示：UID / 主密钥短指纹 / 信任级别 picker / 子密钥列表（含 capability
图标 + 卡上 / stripped 标记）/ 「设为默认」按钮（仅自有私钥）/ 「…」menu
和右键 context menu。

### 钥匙操作

- **新建密钥**：算法（RSA 4096 / Ed25519 / NIST P-256 等）+ 过期 + passphrase
    + 可选认证子密钥，可选「保存到 SimpleZip 私有」；
- **导入**：从 ASC / GPG 文件导入到 `~/.gnupg/` 或 SimpleZip 私有 ring；
- **从智能卡导入公钥**：检测到卡时一键拉回主密钥公钥；
- **导出公钥** / **导出私钥**（私钥导出有二次确认）；
- **修改 passphrase**（loopback 模式，新 passphrase 走 stdin 不进 `ps`）；
- **添加 User ID**（一把密钥可挂多个邮箱 / 别名）；
- **修改过期时间**；
- **生成撤销证书**（紧急情况下用来宣告密钥作废，生成时建议立即离线保存）；
- **删除密钥**（destructive 二次确认）；
- **信任级别**：unknown / never / marginal / full / ultimate；
- **设为默认签名密钥**。

### 智能卡 / OpenPGP token

设置 → GPG → 高级里开「启用智能卡 / OpenPGP token 支持」后，UI 多出：

- 顶部「插入的智能卡」面板：vendor / serial / 跟哪把主密钥绑定；
- 钥匙串里多出「智能卡」分组；
- 「从智能卡导入公钥」操作按钮。

检测靠 `gpg --card-status --with-colons` 权威解析。

### Passphrase 处理

SimpleZip 主流程下**不碰**你的私钥 passphrase，所有 `gpg --sign` /
`gpg --verify` 都走 `gpg-agent` + `pinentry-mac` 的原生 macOS 密码框。
**例外**：创建密钥 / 修改 passphrase 的 loopback 流程会让你在 SimpleZip 的
SecureField 里输入 —— 这是因为 pinentry-mac 在 GUI App 进程环境下不稳定，
是用「可靠的 SimpleZip 自己收 → 立刻喂给 gpg → 不存任何地方」换取的权衡。

## 🛡 安全边界

SimpleZip 目前**不**是沙盒 App —— 它要浏览 / 管理用户文件、跑 CLI 后端、
挂 DMG、拖拽导出、打开临时解出的文件。所以信任边界必须明确：**下载来的所有
压缩包都视为不可信输入**。

当前已有的保护（完整威胁模型见 [SECURITY.zh-CN.md](./SECURITY.zh-CN.md)）：

- 解压先进临时目录，再合并到目标 —— 同名冲突由 SimpleZip 处理，不让后端
  静默覆盖；
- 可疑路径（`../` / 绝对 / Windows 盘符 / UNC）触发「设置 → 压缩 → 安全 →
  可疑路径」策略门；
- staging 里的符号链接触发「符号链接」策略门，确认前不合并 / 不打开；
- 从压缩包里打开 `.app` / `.pkg` / 脚本 / HTML / Office 等主动内容触发
  「主动内容」策略门；
- 密码走 stdin / PTY，**不**作为可见命令行参数，不出现在 `ps`；
- DMG 走 `hdiutil`，浏览 / 解压时只读挂载；
- **预设密码**：存 Keychain（`kSecAttrAccessibleAfterFirstUnlock`），永远
  不进 `UserDefaults`；进程内只缓存到 App 退出；明文展示要 Touch ID
  / 登录密码确认；
- `.siz` tar 层加固：拒绝非常规文件条目、拒绝第四个文件、拒绝预期三件套
  之外的文件、拒绝不安全的 `innerArchiveName`。

三个策略门（可疑路径 / 符号链接 / 主动内容）每个都可以改成「**始终拒绝**」，
共享 / 公共机器上推荐这么干。

## 🗃 文件管理（本地）

普通文件夹列表基本就是个原生文件管理器，支持的操作：

- 打开 / 用…打开（Open With）/ 快速查看（空格）/ 显示简介（⌘I）；
- 文件夹**原位展开**（行首箭头，像 Finder 列表视图；可在设置 → 浏览器里关掉）；
- 添加到压缩包 / 转换格式 / 拆分为分卷 / 合并分卷（选中 `.001` 首卷时）；
- 解压到此处 / 测试压缩包；
- **哈希**（CRC32 / MD5 / SHA1 / SHA256 / SHA512）；
- 新建文件 / 文件夹（内联重命名）；
- 重命名（选中按 Return 内联改名）/ 创建副本（⌘D）/ 创建符号链接；
- 复制 / 剪切 / 粘贴 / 移动到 / 删除到废纸篓；
- **查看 / 修改 Unix 权限与属主**（右键 → 权限与所有者，chmod / chown）；
- **撤销 / 重做**（⌘Z / ⇧⌘Z）—— 移动、重命名、复制、副本、删除、新建、拆分、
  合并、转换、改权限都能撤销；撤销采用「移废纸篓可恢复」「移回原位」等保守方式，
  且每步都校验「源未被外部改动、目标空闲」，绝不静默覆盖；
- 拖拽移动或复制 / 多选 / 标签视图。

同名冲突（粘贴 / 拖放 / 解压 / 创建）走**同一个对话框**：

- 替换 / 保留两者 / 跳过；
- **仅当内容不同时替换** —— 比对双方 SHA256，相同则跳过、不同才覆盖；
- 文件夹冲突额外有两种模式开关：**合并（Finder 风格，保留目标里多余的文件）**
  与 **替换整个文件夹（tar 风格，整体替换）**，可「应用到本次操作的其余冲突」；
- 覆盖一律「写临时 → 成功后原子替换」，创建 / 覆盖失败都不会连旧带新都丢。

## 🍎 Finder 右键服务

SimpleZip 把右键动作注册成 macOS 系统服务（NSServices）。在 Finder 里选中任意
文件 / 文件夹后右键，在 **服务**（或直接出现在右键菜单）里能看到「… with SimpleZip」：

- **计算哈希** —— 跳到 SimpleZip 的哈希窗口算 CRC32 / MD5 / SHA1 / SHA256 / SHA512；
- **添加到压缩包** —— 直接进入创建压缩包流程，目标默认是源文件所在目录；
- **解压 / 解压到此** —— 用轻量进度浮窗把选中的压缩包各自解开；
- **创建 ZIP / 7z / TAR.GZ** —— 按默认设置一键出包，无对话框。

这些服务由主 App 提供，菜单标题按系统语言本地化（`SimpleZip/*.lproj/ServicesMenu.strings`）。
首次使用若没出现，在 系统设置 → 键盘 → 键盘快捷键 → 服务 里勾选启用即可。
（早期版本曾用一个独立的 Finder Sync 扩展实现这些动作，现已移除，统一走系统服务。）

## 🔗 文件关联

设置 → 文件关联里按格式单独设置默认打开方式。当前覆盖：

- `.zip` / `.7z` / `.tar` / `.gz` / `.tgz` / `.bz2` / `.xz` / `.rar` / `.dmg`；
- `.001` 分卷组（7z / zip 分卷）；
- `.z01` 分卷 ZIP 组；
- `.r00` RAR 分卷组；
- `.siz` SimpleZip 签名容器。

每行显示当前默认 App + 「设为默认」按钮。

## ⚙️ 设置

按面板分：

- **通用**：启动位置（含自定义路径 + 失效弹窗）/ 记住上次文件夹 / 界面语言
  / 预设密码 / 重新运行欢迎助手 / 偏好备份 / 还原；
- **压缩**：7-Zip 后端 / RAR 后端 / 安全策略（可疑路径 / 符号链接 / 主动内容）
  / Finder 自动解压 / 同名文件默认策略 / **默认压缩设置**（每种格式的级别 / 加密
  等，相当于压缩预设）；
- **浏览器**：是否显示隐藏文件 + 判定方式 / 隐藏组折叠策略 / 显示符号链接 /
  **文件夹原位展开**开关；
- **列表列**：本地文件列表 + 压缩包浏览各自的可见列（权限 / 属主 / 注释 / 符号链接
  目标等）；
- **文件关联**：按扩展名设默认 App；
- **软件更新**：检查新版本、在 App 内读发行说明（从 GitHub 拉 CHANGELOG，按版本
  折叠）；
- **GPG**：主开关 / 智能卡支持 / 钥匙串管理（五分组）/ 默认值子段（签名密钥
  策略）/ 高级（后端路径 / pinentry-mac / GNUPGHOME / SimpleZip 私有 ring 路径）；
- **运行状态**：首启动 / 任意时刻的诊断面板（后端 / GPG / 加密临时卷 / Finder 服务
  / 启动位置 / 更新配置等接线检查）+ 一键导出诊断信息；
- **备份与还原**：偏好导出（json） / 导入（patch 语义） / 还原为默认值。

切语言后，**重启 SimpleZip** 才完整生效（顶部菜单栏跟主体内容一起翻）。

## ❓ 常见问题

### 为什么 7z / RAR 打不开？

设置 → 压缩里看一下后端路径和版本。7z / RAR 浏览 / 解压依赖 7-Zip；RAR
创建额外需要 RARLAB `rar`。

```bash
brew install sevenzip
./scripts/install_rar_backend.sh
```

### 怎么在不解压的情况下修改压缩包里的文件？

「双击打开」只是临时解压 + 交给默认 App 查看，改了**不会**自动回到压缩包。要把
改动留在包里，对可编辑的 ZIP / 7z 用**右键 → 编辑**：保存后 SimpleZip 会把内容
写回压缩包（先在临时副本上重打包、再原子替换原包，失败原包字节不变）。你也能往
打开的压缩包里右键新建 / 粘贴 / 重命名 / 删除条目，或直接把外部文件拖进窗口。
加密包会先要口令再写回。

### 能把一个压缩包转换成别的格式吗？

能。右键选中的压缩包 → **转换格式**，选目标格式 / 级别 / 口令即可（例如 `.rar`
转 `.7z`、`.zip` 转加密 `.siz`）。转换 = 解压到临时目录再用目标格式重打包，源包
保持不动；产物撞同名走统一冲突对话框。

### 怎么对比两个压缩包？

选中**两个**受支持的压缩包 → 右键 **对比压缩包**（或选一个再挑另一个），会列出
新增 / 删除 / 改动（类型 / 大小 / CRC / 修改时间 / 加密状态）/ 未变的条目。

### 为什么 `.siz` 验签显示「公钥已导入但未信任」？

意思是签名密码学有效，但你这把公钥的 ownertrust 不到 marginal（通常是
导入了别人公钥但还没给它评级）。去 GPG pane 里把这把公钥的信任级别拉到
marginal / full / ultimate（自己的密钥用 ultimate）。

0.1.7 之前有个 bug：哪怕你已经设了 ultimate，验签 sheet 还是会显示「未信任」
—— 那是因为旧版用 stderr 文本匹配判断信任，受 locale 和 trustdb 状态影响
不稳定。0.1.8 起切到 `gpg --status-fd 1` 机器可读状态，已经修了。

### 为什么 `.siz` 文件加密了但只有签名？

`.siz` v2 是签名容器，**不是加密容器**。要加密两种选法：

- 沿用内层 archive 自带的加密（ZIP AES-256 / 7z 头加密）；
- 用 0.1.9 起的 **`.siz` v3 多收件人加密**：在创建面板里勾「加密内层 archive」，
  填收件人 fingerprint 或对称口令（也可组合）。详情见
  [`.siz` v3 多收件人加密](#siz-v3-多收件人加密019)。

### 为什么分卷压缩包从 Finder 双击不一定进 SimpleZip？

要看 Launch Services 文件关联。去设置 → 文件关联里把对应分卷组设为默认，
或者用 SimpleZip 的「打开压缩包」从任意分卷打开。

### 为什么界面有些文字没翻译？

请确保已经切到目标语言并**重启** SimpleZip。如果重启后某个具体文案还是没
翻译，欢迎在 GitHub 提 issue 附 key 截图。

### 为什么某些格式只能压缩单个文件？

GZip / BZip2 / XZ 本身就是单文件压缩格式。多文件请用 ZIP / 7z / TAR /
TAR.GZ / RAR。

### RAR 后端为什么不直接随 App 分发？

RARLAB `rar` 是专有 / shareware 软件。脚本会装到用户数据目录，不会进 App
包；公开分发带 RAR 后端的 App 包前请确认你有 RARLAB 再分发授权。

### 在哪里看更详细的安全说明？

完整威胁模型 / 安全控件 / `.siz` 容器设计：
[SECURITY.zh-CN.md](./SECURITY.zh-CN.md)。
